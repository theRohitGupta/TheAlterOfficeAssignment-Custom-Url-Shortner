import Router from 'express';
import { getShortUrlByCustomAlias } from '../controllers/short-url/get-by-custom-alias';

const router = Router();

// router.get('/topic/:topic', getShortUrlByCustomAlias)

// router.get('/overall', getShortUrlByCustomAlias)

// router.get('/', getShortUrlByCustomAlias)

export default router